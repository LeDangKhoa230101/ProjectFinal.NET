@model IEnumerable<ProjectFinal.NET.Data.Category>

@{
    ViewData["Title"] = "CategoryManager";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div style="padding: 0 16px;">
    <h2>Category Manager</h2>

    <p>
        <a asp-action="Create" style="text-decoration: none;font-size: 20px;color: blue;">Create New</a>
    </p>

    <table class="table" style="width: 100%;">
        <thead>
            <tr>
                <th style="text-align: left; margin-right: 44px; border-bottom: 2px solid red;">
                    @Html.DisplayNameFor(model => model.IdCate)
                </th>
                <th style="text-align: left; margin-right: 44px; border-bottom: 2px solid red;">
                    @Html.DisplayNameFor(model => model.CateName)
                </th>
                <th style="border-bottom: 2px solid red;"></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.IdCate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.CateName)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.IdCate">Edit</a> |
                        <a asp-action="DetailManager" asp-route-id="@item.IdCate">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.IdCate">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>